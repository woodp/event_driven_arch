{
  "name": "event-driven-arch",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/event-driven-arch/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/event-driven-arch",
        "main": "apps/event-driven-arch/src/main.ts",
        "tsConfig": "apps/event-driven-arch/tsconfig.app.json",
        "assets": ["apps/event-driven-arch/src/assets"],
        "isolatedConfig": true,
        "webpackConfig": "apps/event-driven-arch/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "event-driven-arch:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "event-driven-arch:build:development"
        },
        "production": {
          "buildTarget": "event-driven-arch:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/event-driven-arch/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/event-driven-arch/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    }
  },
  "tags": []
}
